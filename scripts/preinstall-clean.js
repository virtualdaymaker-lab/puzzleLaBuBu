// Deprecated wrapper for compatibility. The real cleanup script is
// provided as a CommonJS file `preinstall-clean.cjs` so it runs
// correctly when the project uses "type": "module".
console.log('[preinstall-clean.js] deprecated — using preinstall-clean.cjs instead');
process.exit(0);
